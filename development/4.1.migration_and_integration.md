Optimize the message layer and gateway workflow with these strict rules:

1. ARCHITECTURE PRINCIPLE (MANDATORY)
- short_term.py = internal upstream storage (single source of truth)
- message.py = ONLY public interface for history and message processing
- All downstream modules in gateway CANNOT import short_term.py directly
- All downstream MUST use message.py for ALL history/message operations

2. REFACTOR TASKS
- Move ALL message processing and history-fetching logic from downstream gateway files INTO message.py
- Keep only clean, unified, reusable functions/classes in message.py
- Remove ALL duplicate code (get history, process messages, turn handling, event appending)
- Ensure NO downstream files directly access short_term.py
- Make message.py the single entry point for message/history operations

3. CODE CHANGES
- Keep all existing behavior, business logic, and event types unchanged
- Keep short_term.py as internal storage only (no breaking changes)
- Expose clean, stable public APIs from message.py for downstream
- Remove redundancy, duplication, and direct dependencies on short_term from downstream

4. FINAL STATE
- Downstream imports ONLY from message.py
- short_term is called ONLY inside message.py
- All history/message logic is centralized in message.py
- Clean, layered, maintainable, production-ready



# 优化消息层与网关工作流（严格遵循以下规则）

## 1. 架构原则（强制要求）

- `short_term.py` = 内部上游存储（唯一真实数据源）
- `message.py` = **仅作为**消息处理与历史记录的公共接口
- 网关内所有下游模块**禁止直接导入** `short_term.py`
- 所有下游模块执行任何历史记录 / 消息操作时，**必须通过 `message.py`**

## 2. 重构任务

- 将下游网关文件中**所有消息处理、历史记录获取逻辑**，全部迁移至 `message.py`
- 在 `message.py` 中仅保留简洁、统一、可复用的函数 / 类
- 移除**所有重复代码**（获取历史记录、消息处理、回合处理、事件追加等）
- 确保**无任何下游文件直接访问** `short_term.py`
- 将 `message.py` 作为消息 / 历史记录操作的**唯一入口**

## 3. 代码变更要求

- 保持现有行为、业务逻辑、事件类型**完全不变**
- 保持 `short_term.py` 仅作为内部存储使用（不做破坏性变更）
- 由 `message.py` 对外提供**简洁、稳定的公共 API**，供下游调用
- 消除冗余代码、重复逻辑，以及下游对 `short_term` 的直接依赖

## 4. 最终目标状态

- 下游模块**仅从 `message.py` 导入**
- `short_term` 仅在 `message.py` 内部被调用
- 所有消息 / 历史记录逻辑**统一集中在 `message.py`**
- 架构分层清晰、易于维护、可直接用于生产环境