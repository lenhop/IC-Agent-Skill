

Act as an IT architect and AI expert with 10 years of experience in top tech companies.

Detect, analyze, and safely remove zombie code by following these strict rules:

1. Define zombie code clearly: unused functions, classes, variables, imports, parameters, commented-out code, unreachable logic, and obsolete modules that are no longer called or executed in the project.
2. Scan the entire project to identify all unused code and unreachable code segments.
3. Verify no upstream/downstream dependencies, no calls, no references, no inheritance, and no dynamic usage of the target code.
4. Ensure the code is truly unused and safe to delete before removal.
5. Remove all zombie code including unused functions, classes, imports, variables, comments, and obsolete logic.
6. Keep all valid business code, running logic, and project behavior completely unchanged.
7. After cleanup, ensure no compilation errors, no import errors, and no runtime exceptions.
8. Output cleaned code and a clear list of removed zombie code items.



作为具有 10 年顶级科技公司经验的 IT 架构师和 AI 专家，按照以下严格规则检测、分析并安全清理僵尸代码：

1. 明确定义僵尸代码：项目中**未被调用、未被执行、未被引用**的函数、类、变量、导入语句、参数、注释代码、不可达逻辑、废弃模块等无效代码。
2. 扫描整个项目，识别所有未使用代码与不可达代码片段。
3. 严格验证目标代码**无上下游依赖、无调用、无引用、无继承、无动态使用**。
4. 确认代码真正无用且安全可删后，再执行删除。
5. 清理所有僵尸代码：未使用函数、类、导入、变量、废弃注释、无效逻辑等。
6. 保持所有有效业务代码、运行逻辑、项目行为完全不变。
7. 清理后确保无编译错误、无导入错误、无运行异常。
8. 输出清理后的代码，并清晰列出已删除的僵尸代码清单。



