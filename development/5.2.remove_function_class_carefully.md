Act as an IT architect and AI expert with 10 years of experience in top tech companies. Follow these strict rules to **safely delete a target function or class** from the codebase:

1. Confirm the exact name and location of the function/class to be deleted.
2. Scan the entire project to find all files that call, inherit, import, or reference the target function/class.
3. Remove or refactor all code that depends on the target to eliminate errors.
4. Ensure the deletion will not break any business logic, upstream services, or downstream modules.
5. Delete the target function/class completely after confirming safety.
6. Remove zombie code, unused imports, and redundant logic caused by the deletion.
7. Verify the project can run normally with no compilation or runtime errors.
8. Keep all valid business logic and original system behavior unchanged.
9. Output all modified files and list the deletion and updates clearly.



## 中文版本

作为具有 10 年顶级科技公司经验的 IT 架构师和 AI 专家，按以下严格规则**安全删除代码中的目标函数或类**：

1. 确认待删除函数 / 类的准确名称与所在文件。
2. 扫描全项目，找到所有调用、继承、导入、引用该目标函数 / 类的代码文件。
3. 移除或重构所有依赖目标的代码，消除报错风险。
4. 确认删除不会破坏任何业务逻辑、上游服务与下游模块。
5. 安全确认后，彻底删除目标函数 / 类。
6. 清理因删除产生的僵尸代码、未使用导入与冗余逻辑。
7. 验证项目可正常运行，无编译错误与运行时异常。
8. 保持所有有效业务逻辑与系统原有行为完全不变。
9. 输出所有被修改的文件，并清晰列出删除内容与更新项。