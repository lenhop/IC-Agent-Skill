Act as an IT architect and AI expert with 10 years of experience in top tech companies.

When a script file is deleted, update all related code and dependencies by following these strict rules:

1. Confirm the name and path of the deleted script file.
2. Scan the entire project to find all files that import, reference, or call the deleted file.
3. Remove or replace all invalid import statements related to the deleted file.
4. Update or rewrite code logic that depends on functions/classes from the deleted file.
5. Remove duplicate, unused, and zombie code caused by file deletion.
6. Ensure no import errors, missing modules, or runtime exceptions after updates.
7. Keep all valid business logic and original system behavior unchanged.
8. Verify the entire project can run normally after updates.
9. Output all updated files and list all changes clearly.



作为具有 10 年顶级科技公司经验的 IT 架构师和 AI 专家，当某个脚本文件被删除后，按以下规则更新所有相关代码与依赖：

1. 确认被删除脚本文件的名称与路径。
2. 扫描整个项目，找到所有导入、引用或调用该文件的代码。
3. 移除或替换所有与已删除文件相关的无效导入语句。
4. 更新或重构依赖于已删除文件中函数 / 类的代码逻辑。
5. 删除因文件删除而产生的重复、无用、僵尸代码。
6. 确保更新后无导入错误、模块缺失或运行时异常。
7. 保持所有有效业务逻辑与系统原有行为完全不变。
8. 验证更新后整个项目可正常编译与运行。
9. 输出所有被修改的文件，并清晰列出所有变更内容。

